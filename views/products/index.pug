extends ../layout/common

block content
  h1 Products

  .btn-toolbar.mt-2.d-flex.justify-content-end
    form.form-inline(action="products/search" method="get")
      .form-group
        input.form-control(type="text", name="q", value= search)
        button.btn.btn-primary.ml-2(type="submit") Search
  .row
    each product in products
      .col-12.col-sm-6.col-md-4.col-lg-3.mt-2
        .card(style="height: 470px;")
          img.card-img-top(src=product.image, alt="...")
          .card-body
            h5.card-title=product.name
            p.card-text.text-justify=product.description
            a.btn.btn-primary(href='/cart/add/' + product.id) Add to cart